using API.Data.Entidades.Gestion.Nomencladores;
using API.Domain.Validators.Gestion.Nomencladores;

namespace API.Domain.Interfaces.Gestion.Nomencladores
{
    public interface IReviewService 
    {
        //Task<IReadOnlyList<Review>> ListByProductAsync(Guid productId, int page, int size, CancellationToken ct = default);
        //Task<double> GetAverageAsync(Guid productId, CancellationToken ct = default);
        //Task<Review> UpsertAsync(Guid userId, Guid productId, int rating, string title, string content, bool isVerified, CancellationToken ct = default);
        //Task<bool> DeleteAsync(Guid userId, Guid productId, CancellationToken ct = default);
    }
}